# Python
__pycache__/
*.pyc

# OS / 编辑器
.DS_Store
Thumbs.db
*.tmp
~$*
nul

# Word 临时文件
法典源/~$*

# 新放入的源 docx 只作为本地转换输入；转换后的 JSON 才进仓库
法典源/*.docx

# 已转换源文档归档（不再参与转换；本地留存，不进仓库）
法典源_已转换/

# 原图（本地保留、不进仓库；将来批量上传 R2）
originals/

# 图片缓存（实际发布到 Cloudflare R2，不进仓库）
site/images/
image.png

# Cloudflare R2 本地密钥配置
r2_config.json
.r2_sync_manifest.json

# 本地发布日志
publish.log

# 临时转换/对比目录
output/
新版本待转/

# 本地 AI 交接说明（不公开；供新会话 / 其它 AI 快速接手）
CLAUDE.md
.claude/
